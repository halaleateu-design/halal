{
  "name": "go-halal-monorepo",
  "private": true,
  "description": "Render entry — API lives in backend/",
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "build": "npm install --prefix backend",
    "start": "npm start --prefix backend",
    "test:e2e": "node scripts/run-e2e-automated.mjs"
  },
  "engines": {
    "node": "22.10.0"
  }
}
