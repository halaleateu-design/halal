GO + Halal eat EU — Render + Strong SSO (Google)
================================================

PART 1 — RENDER (API live)
--------------------------
1) Double-click: GITHUB-AUR-RENDER.bat
   (GitHub repo + Render deploy — ek dafa)

2) Render par service name: go-api
   Root Directory: backend

3) Environment variables (Render dashboard):
   JWT_SECRET          = auto ya lambi string
   ADMIN_API_KEY       = apni secret key
   CORS_ORIGIN         = https://fanciful-moxie-6b5bba.netlify.app
   FRONTEND_URL        = https://fanciful-moxie-6b5bba.netlify.app
   API_PUBLIC_URL      = https://YOUR-SERVICE.onrender.com
   DATABASE_PATH       = ./data/go.db
   UPLOAD_DIR          = ./data/uploads

4) Deploy ke baad test:
   https://YOUR-SERVICE.onrender.com/api/v1/health

5) Netlify connect:
   scripts\set-render-api.ps1 chalao with Render URL
   phir DEPLOY-WEBSITE.bat


PART 2 — GOOGLE SSO (strong login)
----------------------------------
1) https://console.cloud.google.com
2) New project: GO Halal EU
3) APIs & Services → OAuth consent screen → External → fill app name GO
4) Credentials → Create OAuth client ID → Web application
5) Authorized redirect URIs (EXACT):
   https://YOUR-SERVICE.onrender.com/api/v1/auth/google/callback

6) Copy Client ID + Client Secret → Render Environment:
   GOOGLE_CLIENT_ID
   GOOGLE_CLIENT_SECRET
   GOOGLE_REDIRECT_URI = same callback URL above

7) Render → Manual Deploy (ya wait for auto)

8) Test: signin.html → Continue with Google


PART 3 — WEBSITE
------------------
DEPLOY-WEBSITE.bat → Netlify drag folder


PART 4 — BAQI (boosting)
------------------------
- Google Search Console → sitemap submit
- Google Business Profile (Porto)
- Instagram @eathalaleu bio mein site link

Apple Sign-In = baad mein (Apple Developer $99/year)
